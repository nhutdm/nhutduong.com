---
import type { HTMLAttributes } from 'astro/types';

type Props = HTMLAttributes<'a'>;

const { class: className, ...rest } = Astro.props;
---

<a class:list={['hover:text-foreground transition-colors', className]} {...rest}>
  <slot />
</a>
